import { Container, Form, Nav, Main } from './styles';

const Layout = () => {
  return (
    <Container>
      <Form>form</Form>
      <Nav>nav</Nav>
      <Main>main</Main>
    </Container>
  )
}

export default Layout
